<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shoebid.www.repository.BidDAO">
	<insert id="insert" parameterType="bvo">
		insert into buy_bid
		(buyer, pno, bid_price, bid_status)
		values (#{buyer}, #{pno}, #{bidPrice}, #{bidStatus})
	</insert>
	<select id="selectList" resultType="bvo">
		select b.bno, b.buyer, b.pno, p.pname, p.product_img, b.bid_price, b.bid_status, b.bid_at
		from (
			select bno from buy_bid where bno > 0
			order by bno desc
			limit #{pgvo.pageStart}, #{pgvo.qty}
		) a left join buy_bid b on (a.bno = b.bno) 
		inner join product p on (b.pno = p.pno)
		where b.buyer = #{mno}
		order by bid_at desc;
	</select>
	<select id="selectTotalCount" parameterType="long" resultType="int">
		select count(bno) from buy_bid where buyer = #{buyer}
	</select>
	<update id="updateBidStatus" parameterType="bvo">
		update buy_bid set bid_status = #{bidStatus}
		where bno = #{bno}
	</update>
</mapper>